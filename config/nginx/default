# GENERATED BY PUPPET
server {
        listen   80; ## listen for ipv4; this line is default and implied
        #listen   [::]:80 default_server ipv6only=on; ## listen for ipv6

        server_name local-formulaires.example.net;

        root /usr/share/nginx/www;
        index index.html index.htm;

        location /static { alias /var/lib/wcs-au-quotidien/collectstatic/; }
        location / {
            include scgi_params;
            scgi_pass localhost:3001;
            scgi_param SCRIPT_NAME  '';
            scgi_param PATH_INFO  $uri;
#            scgi_param HTTPS yes;
        }

        error_log /var/log/nginx/error-local-formulaires.example.net.log warn;
        access_log /var/log/nginx/access-local-formulaires.example.net.log combined;

}
server {
        listen   80;

        location ~ ^/static/(.+)$ {
            root /;
            try_files /var/lib/combo/tenants/$host/static/$1
                      /var/lib/combo/tenants/$host/theme/static/$1
                      /var/lib/combo/collectstatic/$1
                      =404;
            add_header         'Access-Control-Allow-Origin' '*';
        }

        location ~ ^/media/(.+)$ {
            alias /var/lib/combo/tenants/$host/media/$1;
        }

        server_name local.example.net;

        location / {
            proxy_pass         http://unix:/var/run/combo/combo.sock;
            proxy_set_header   Host $http_host;
            proxy_set_header   X-Real-IP       $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        error_log /var/log/nginx/error-local.example.net.log warn;
        access_log /var/log/nginx/access-local.example.net.log combined;
}
server {
        listen   80;

        location ~ ^/static/(.+)$ {
            root /;
            try_files /var/lib/combo/tenants/$host/static/$1
                      /var/lib/combo/tenants/$host/theme/static/$1
                      /var/lib/combo/collectstatic/$1
                      =404;
        }

        location ~ ^/media/(.+)$ {
            alias /var/lib/combo/tenants/$host/media/$1;
        }

        server_name local-portail-agent.example.net;

        location / {
            proxy_pass         http://unix:/var/run/combo/combo.sock;
            proxy_set_header   Host $http_host;
            proxy_set_header   X-Real-IP       $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        error_log /var/log/nginx/error-local-portail-agent.example.net.log warn;
        access_log /var/log/nginx/access-local-portail-agent.example.net.log combined;
}

server {
        listen   80;

        server_name local-auth.example.net;

        location ~ ^/static/(.+)$ {
            root /;
            try_files /var/lib/authentic2-multitenant/static/$1
                      /var/lib/authentic2-multitenant/collectstatic/$1
                      =404;
        }

        location / {
            rewrite            ^/accounts/saml/login/ /accounts/fedict/login/ last;
            proxy_pass         http://unix:/run/authentic2-multitenant/authentic2-multitenant.sock;
            proxy_set_header   Host $http_host;
            proxy_set_header   X-Real-IP       $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        error_log /var/log/nginx/error-local-auth.example.net.log warn;
        access_log /var/log/nginx/access-local-auth.example.net.log combined;
}
server {
        listen   80;

        server_name local-documents.example.net;

        location ~ ^/static/(.+)$ {
            root /;
            try_files /var/lib/fargo/static/$1
                      /var/lib/fargo/collectstatic/$1
                      =404;
        }

        location / {
            proxy_pass         http://unix:/var/run/fargo/fargo.sock;
            proxy_set_header   Host $http_host;
            proxy_set_header   X-Real-IP       $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        error_log /var/log/nginx/error-local-documents.example.net.log warn;
        access_log /var/log/nginx/access-local-documents.example.net.log combined;
}
server {
        listen   80;

        server_name local-passerelle.example.net;

        location ~ ^/static/(.+)$ {
            root /;
            try_files /var/lib/passerelle/static/$1
                      /var/lib/passerelle/theme/static/$1
                      /var/lib/passerelle/collectstatic/$1
                      =404;
        }

        location / {
            proxy_pass         http://unix:/var/run/passerelle/passerelle.sock;
            proxy_set_header   Host $http_host;
            proxy_set_header   X-Real-IP       $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        error_log /var/log/nginx/error-local-passerelle.example.net.log warn;
        access_log /var/log/nginx/access-local-passerelle.example.net.log combined;
}
